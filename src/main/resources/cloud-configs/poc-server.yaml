
server:
  port: 8011

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

demo:
  color: blue
  attitude: high
  #relaxed-binding: relaxed with dash
  relaxed_binding: relaxed with undercore
  #relaxedBinding: relaxed camel case